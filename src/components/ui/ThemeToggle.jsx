// ThemeToggle — hidden in liquid glass dark-only mode
// Kept as a no-op component to avoid import errors elsewhere
export default function ThemeToggle({ className = '' }) {
  // Dark mode only — no toggle needed
  return null;
}
